#include<stdio.h> 

int main()
{
    /*
        1.Compile time error 
          syntax errors 
          printf("Hello world") => compile time error   

        2.Logical Error (bug)
            a.Compilation => Yes 
            b.Execution => Yes 
            c.Successfull execution => Yes 
            Incorrect o/p (wrong o/p) 

        3.Runtime Error (program fail)
          a. Complilation => yes 
          b. Execution => Yes 
          C. Successfull execution => no          
    
    */
    return 0;  
}

/*

    1. Revise the concepts (refer recorded session)
    2. Prepare the notes and revise it 
    3. Read the Blackbook (sunbeam book ) in parllel

*/