#include<stdio.h> 

int main()
{
    //jump statements 
    //break , continue , goto , return 

    //break 

    // int i = 1; 
    // if(i == 1)
    // {
    //     break; //break statement not within loop or switch
    // }

    int x = 1; 
    if(x == 1)
    {
        //continue; //continue statement not within a loop
    }
    //continue will take the control to the nearest loop 
    return 0;
}
